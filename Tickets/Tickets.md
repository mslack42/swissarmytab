(I'll probably split each of these into their own md files, and delete as each one is implemented)

## Editors

- Diff check (L)
- xpath test (S)
- Case convert (M)
- Regex\* (XXL) - you really should just use regex 101 though
- Guid generation (M)
- XSLT testing (XXL)
- XML (and the conversion to/from JSON) (L)
- Markdown (L)
- Stopwatch, timers, etc. (L)
- RNG (L)
- Epoch timestamp stuff (L)
- Timezone stuff (XL)
- Unicode tableflip library (L)
- JS object (vs. JSON object) (XL)

## Context menu actions

- Remove whitespace (S)
- JSON escape / unescape (S)
- Keep (delete everything else in this box) (M)
- Case converts (S)
- Generate guid here (S)
- Transforms (e.g. JSON <-> XML) (M)

## Other features

- Initial set-up for context menu configurability and application (XXXL)
- Data storage editor (XXXL) - opens from top bar
- Set up toaster (L)
- Generic solution that compares tab data to storage data; in the case of mismatch, warn the user

## Misc.

- Keep Awake should use fake video for non-mobile (L)
- Toast error on tab rename failure (S)
- Copy to clipboard button (M)
- Keep Awake text-hiding for the script example, and copy-to-clipboard functionality
- New user welcome features
- Beta features user flag
- Buttons should be buttons everywhere, with anims

## PWA features

Install button
Update Banner
